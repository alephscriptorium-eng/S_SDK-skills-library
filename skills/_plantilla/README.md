# plantilla-skill

Stub de layout. Copiá este directorio a `skills/<nombre>/` y rellená
`SKILL.md` (frontmatter `name` + `description` obligatorios).

No es un skill operativo: solo valida el formato del paquete.
