# BACKLOG (fixture SINTÉTICA) — la tabla no declara `deps` ni `ejes`

Los siete campos se exigen por WP. Una tabla que ni siquiera tiene columna para
`deps` o `ejes` no puede declararlos: no es despachable aunque sus filas se
lean bien.

## Lane A · ALFA

| WP | P | BRIEF | CA |
| -- | - | ----- | -- |
| **FX-A01** | P0 | Extraer el kit de plantillas a un paquete propio | el probe del consumidor sintetico resuelve la plantilla con exit 0 |
